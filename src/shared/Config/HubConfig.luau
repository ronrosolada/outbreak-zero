--[[
	HubConfig
	---------
	Tunables for the social hub + matchmaking queue.
]]

local HubConfig = {
	SPECTACLE_SECONDS = 3, -- alarm + airlock drama before the roster drops into the arena
	QUEUE_PROMPT = "Queue for Outbreak", -- ProximityPrompt action text on the pad
	QUEUE_OBJECT_TEXT = "Containment Breach", -- ProximityPrompt object text
}

return HubConfig
